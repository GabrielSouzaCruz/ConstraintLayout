<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ConstraintLayout">

        <!-- Tela Principal -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity Blog Post -->
        <activity
            android:name=".BlogPostActivity"
            android:exported="false"
            android:label="Blog Post" />

        <!-- Activity Série -->
        <activity
            android:name=".SerieActivity"
            android:exported="false"
            android:label="Série" />

        <!-- Activity Concurso de Textos -->
        <activity
            android:name=".ConcursoActivity"
            android:exported="false"
            android:label="Concurso de Textos" />

    </application>
</manifest>
